[project]
name = "production-control"
version = "0.1.0"
description = ""
authors = [
    {name = "dedenevn-sketch",email = "dedenevn@gmail.com"}
]
requires-python = "^3.12"
dependencies = [
    "fastapi (>=0.115.0)",
    "uvicorn[standard] (>=0.30.0)",
    "python-multipart (>=0.0.9)",
    "sqlalchemy (>=2.0.0)",
    "asyncpg (>=0.29.0)",
    "alembic (>=1.13.0)",
    "pydantic (>=2.7.0)",
    "pydantic-settings (>=2.3.0)",
    "celery[amqp] (>=5.3.0)",
    "redis (>=5.0.0)",
    "minio (>=7.2.0)",
    "python-dotenv (>=1.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
